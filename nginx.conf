server {
    listen 80;
    server_name 81.26.186.21;

    location /static/ {
        root /usr/share/nginx/html;
    }


    location / {
        proxy_pass http://web:8000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}